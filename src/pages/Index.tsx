import DiscoverHomePage from "@/pages/DiscoverHomePage";

/** Public home: app-style discover only (see `product-spec/home.md`). */
const Index = () => {
  return <DiscoverHomePage />;
};

export default Index;
